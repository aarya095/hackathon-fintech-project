# Required by Prisma (schema uses env("DATABASE_URL"))
# Use an absolute path in Docker (e.g. file:/app/data/dev.db).
DATABASE_URL="file:./prisma/dev.db"

# Auth (required in production)
JWT_SECRET="change-me-in-production"

# CORS – set to your app's public URL (e.g. https://app.example.com).
# This is the single env var for allowed origin.
CORS_ORIGIN="http://localhost:5173"

# Optional
PORT=3000
